name: terraform-module-keyvaultcertificates

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - .github/workflows/terraform-module-keyvaultcertificates.yml
      - .github/template/build-terraform-module/**
      - infra/terraform/modules/keyvaultcertificates/**
  pull_request:
    branches: ["main"]
    paths:
      - .github/workflows/terraform-module-keyvaultcertificates.yml
      - .github/template/build-terraform-module/**
      - infra/terraform/modules/keyvaultcertificates/**

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: "./.github/template/build-terraform-module"
        with:
          terraformPath: infra/terraform
          terraformModulePath: modules/keyvaultcertificates

  Publish:
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: "./.github/template/deploy-terraform-module"
        with:
          terraformModulePath: modules/keyvaultcertificates
